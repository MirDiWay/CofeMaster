<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	<Style TargetType="ListView">
		<Setter Property="FocusVisualStyle" Value="{x:Null}"/>
		<Setter Property="VirtualizingPanel.ScrollUnit" Value="Pixel"/>
		<Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
		<Setter Property="Background" Value="Transparent"/>
		<Setter Property="BorderBrush" Value="Transparent"/>
		<Setter Property="BorderThickness" Value="0"/>
	</Style>

	<Style
	    x:Key="NoSelected"
	    TargetType="ListViewItem">
		<Setter Property="FocusVisualStyle" Value="{x:Null}"/>
		<Setter Property="Margin" Value="15"/>
		<Setter Property="VerticalAlignment" Value="Top"/>
		<Setter Property="HorizontalAlignment" Value="Left"/>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="ListViewItem">
					<Border
					    x:Name="border"
					    Background="Transparent"
					    BorderBrush="Transparent"
					    BorderThickness="0"
					    CornerRadius="0">
						<ContentPresenter
						    HorizontalAlignment="Left"
						    VerticalAlignment="Center"/>
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style
	    x:Key="ListViewItemSelected"
	    TargetType="ListViewItem">
		<Setter Property="FocusVisualStyle" Value="{x:Null}"/>
		<Setter Property="Margin" Value="3.5"/>
		<Setter Property="Cursor" Value="Hand"/>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="ListViewItem">
					<Border
					    x:Name="border"
					    Background="Transparent"
					    BorderBrush="Transparent"
					    BorderThickness="1"
					    CornerRadius="10">
						<Grid>
							<Border
							    x:Name="NeonBorder"
							    BorderBrush="#F8F8F0"
							    Style="{DynamicResource NeonBorder}"
							    Visibility="Collapsed">
								<Border.Effect>
									<DropShadowEffect
									    BlurRadius="25"
									    Direction="0"
									    ShadowDepth="0"
									    Color="#F8F8F0"/>
								</Border.Effect>
							</Border>
							<ContentPresenter/>
						</Grid>
					</Border>
					<ControlTemplate.Triggers>
						<Trigger Property="IsMouseOver" Value="True">
							<Setter TargetName="border" Property="Background" Value="#5A3A2D"/>
							<Setter TargetName="border" Property="BorderBrush" Value="#5A3A2D"/>
							<Setter TargetName="border" Property="BorderThickness" Value="1"/>
						</Trigger>
						<Trigger Property="IsSelected" Value="True">
							<Setter TargetName="border" Property="Background">
								<Setter.Value>
									<RadialGradientBrush ColorInterpolationMode="SRgbLinearInterpolation" MappingMode="Absolute" SpreadMethod="Reflect">
										<GradientStop Offset="1" Color="#5A3A2D"/>
										<GradientStop Color="#5A3A2D"/>
									</RadialGradientBrush>
								</Setter.Value>
							</Setter>
							<Setter TargetName="NeonBorder" Property="Visibility" Value="Visible"/>
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style
	    x:Key="ListViewItemSelectedMunuList"
	    TargetType="ListViewItem">
		<Setter Property="FocusVisualStyle" Value="{x:Null}"/>
		<Setter Property="Margin" Value="15,3.5,15,3.5"/>
		<Setter Property="Cursor" Value="Hand"/>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="ListViewItem">
					<Border
					    x:Name="border"
					    Background="Transparent"
					    BorderBrush="Transparent"
					    BorderThickness="1"
					    CornerRadius="10">
						<Grid>
							<Border
							    x:Name="NeonBorder"
							    BorderBrush="#462AD8"
							    Style="{DynamicResource NeonBorder}"
							    Visibility="Collapsed">
								<Border.Effect>
									<DropShadowEffect
									    BlurRadius="25"
									    Direction="0"
									    ShadowDepth="0"
									    Color="Green"/>
								</Border.Effect>
							</Border>
							<ContentPresenter/>
						</Grid>
					</Border>
					<ControlTemplate.Triggers>
						<Trigger Property="IsMouseOver" Value="True">
							<Setter TargetName="border" Property="Background" Value="#44291e"/>
							<Setter TargetName="border" Property="BorderBrush" Value="#44291e"/>
							<Setter TargetName="border" Property="BorderThickness" Value="1"/>
						</Trigger>
						<Trigger Property="IsSelected" Value="True">
							<Setter TargetName="border" Property="Background">
								<Setter.Value>
									<RadialGradientBrush ColorInterpolationMode="SRgbLinearInterpolation" MappingMode="Absolute" SpreadMethod="Reflect">
										<GradientStop Offset="1" Color="Black"/>
										<GradientStop Color="Black"/>
									</RadialGradientBrush>
								</Setter.Value>
							</Setter>
							<Setter TargetName="NeonBorder" Property="Visibility" Value="Visible"/>
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<!--<Style
	    x:Key="MenuCashItemSelected"
	    TargetType="ListViewItem">
		<Setter Property="FocusVisualStyle" Value="{x:Null}"/>
		<Setter Property="Cursor" Value="Hand"/>
		<Setter Property="Foreground" Value="#9a9a9a"/>
		<Setter Property="VerticalAlignment" Value="Center"/>
		<Setter Property="HorizontalAlignment" Value="Stretch"/>
		<Setter Property="FontFamily" Value="\ContentFolder\FontFolder\#Jura"/>
		<Setter Property="FontSize" Value="40"/>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="ListViewItem">
					<Grid Margin="10,5,10,5">
						<Border
						    x:Name="NeonBorder"
						    BorderThickness="0"
						    Style="{DynamicResource NeonBorder}"
						    Visibility="Collapsed">
							<Border.Effect>
								<DropShadowEffect
								    BlurRadius="25"
								    Direction="0"
								    ShadowDepth="0"
								    Color="#462AD8"/>
							</Border.Effect>
						</Border>
						<ContentPresenter
						    Margin="10"
						    HorizontalAlignment="Left"
						    VerticalAlignment="Center"/>
					</Grid>
					<ControlTemplate.Triggers>
						<Trigger Property="IsMouseOver" Value="True">
							<Setter Property="Background" Value="#2a2a2a"/>
							<Setter Property="Foreground" Value="#a4a4a4"/>
						</Trigger>
						<Trigger Property="IsSelected" Value="True">
							<Setter TargetName="NeonBorder" Property="Background" Value="#462AD8"/>
							<Setter TargetName="NeonBorder" Property="Visibility" Value="Visible"/>
							<Setter Property="Foreground" Value="#f5f5f5"/>
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style
	    x:Key="ListMenuCashItemSelected"
	    BasedOn="{StaticResource NoSelected}"
	    TargetType="ListViewItem">
		<Setter Property="Margin" Value="10"/>
	</Style>-->
</ResourceDictionary>